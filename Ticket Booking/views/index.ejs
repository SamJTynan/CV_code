<!DOCTYPE html>
<html>
<head><meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="style.css">
  <title> Home Page</title>
</head>
<body>
<div>

<a href = "\"><h1 class = "header" >Ticketsaver</h1></a>
<ul class = "taskbar">  <!-- task bar for navigation -->
  <% if (typeof loggedIn !== 'undefined') { %> <!-- Checks if user is logged in  -->
    <li class = "option" ><a href ="\logout">Logout</a></li> <!-- Only shows logout and profile page if the user is logged in -->
    <li class = "option" ><a href ="\ProfilePage">Profile</a></li><% } %>
	<li class = "option" ><a href="\Events">Events</a></li>  <!-- Shows regardless if user is logged in or not -->
</ul>
</div>
</div>
<br><br>


<div class="center"> 
<h1 class = "centretext" >Book tickets</h1> <!-- Text to show in center div -->
<h1 class = "centretext" >online today</h1>
<br>
<br>
<br>
<br>
<% if (typeof loggedIn !== 'undefined') { %>
  <h3><%= loggedIn %></h3> <!-- Shows Event page if user is logged in -->
  <a href ="\Events"> <button class="button">Check out some events!</button></a>

<% } 
// Shows login button if the user isnt logged in button 
else {%>
  <a href ="\login"> <button class="button">Login </button></a> 
<% } %>
</div>

<img class = "display" src="stock-1.jpg">  
<img class = "display" src="stock-2.jpg">

<br>


<script>   //Taken from: https://codepen.io/danielguillan/pen/duiAq  
var current = 0,
    slides = document.getElementsByClassName("display"); //Gets everything under the display class 

setInterval(function() {
  for (var i = 0; i < slides.length; i++) {
    slides[i].style.opacity = 0; 
  }
  current = (current != slides.length - 1) ? current + 1 : 0;
  slides[current].style.opacity = 1;
}, 3000);
</script>
</body>
</html>
